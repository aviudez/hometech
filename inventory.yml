all:
  hosts:
    proxy_iptv:
      ansible_host: 192.168.1.100 # Sustituir por la IP local de tu Pi 3B
      ansible_user: pi            # Sustituir por tu usuario
      ansible_ssh_private_key_file: ~/.ssh/id_rsa # Preferible a usar contraseñas en texto plano
